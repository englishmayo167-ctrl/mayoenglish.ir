// باز و بسته کردن منوی موبایل function toggleSidebar() { const sidebar = document.querySelector(".sidebar"); sidebar.classList.toggle("show"); } // ادامه دوره function openCourse(courseName) { alert( "در حال ورود به دوره «" + courseName + "» هستید.\n\nبخش محتوای دوره در مرحله بعد اضافه می‌شود." ); } // خروج از حساب function logout() { const result = confirm( "آیا می‌خواهید از پنل کاربری خارج شوید؟" ); if (result) { // در نسخه فعلی فقط پیام نمایش داده می‌شود. // بعداً این قسمت به سیستم احراز هویت واقعی متصل خواهد شد. alert("با موفقیت خارج شدید."); window.location.href = "../index.html"; } } // کلیک روی گزینه‌های منو document.querySelectorAll(".menu-item").forEach(function(item) { item.addEventListener("click", function(event) { event.preventDefault(); document.querySelectorAll(".menu-item") .forEach(function(menu) { menu.classList.remove("active"); }); this.classList.add("active"); }); });
